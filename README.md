# GA-artist-project
Use genetic algorithm to regenerate target artworks through iterative optimization
to use this program, change parameters in user config area, which is on the top of main.py:
1. put your target image in the dir and enter the name in the user config
2. enter other parameters like polygon numbers of the output, total generations, etc.
3. then run main.py, a output dir will be generated automatically